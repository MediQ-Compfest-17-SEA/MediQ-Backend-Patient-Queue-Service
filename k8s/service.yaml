apiVersion: v1
kind: Service
metadata:
  name: patient-queue-service
  labels:
    app: patient-queue-service
    component: service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8605"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8605
    targetPort: 8605
    protocol: TCP
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: patient-queue-service
  sessionAffinity: None
---
apiVersion: v1
kind: Service
metadata:
  name: patient-queue-service-headless
  labels:
    app: patient-queue-service
    component: headless-service
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - name: http
    port: 8605
    targetPort: 8605
    protocol: TCP
  selector:
    app: patient-queue-service
